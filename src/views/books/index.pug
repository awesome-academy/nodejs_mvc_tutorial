extends ../layout

block content
  h1= title

  ul
    each book in books
      li
        a(href=book.url) #{book.title}
        |  (#{book.author.fullName})
    else
      p #{t('books.index.no_books')}
